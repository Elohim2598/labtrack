{
    secrets  => ['labtrack-change-this-in-production-2026'],
    database => {
        dsn      => $ENV{DATABASE_URL} // 'dbi:Pg:dbname=labtrack;host=localhost',
        username => $ENV{DB_USER}      // 'postgres',
        password => $ENV{DB_PASS}      // '',
    },
}
